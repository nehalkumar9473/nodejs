/*const fs = require('fs');
//fs.writeFileSync('notes.txt', "Its my first Node.js Code");

// Now append some data in existing file

fs.appendFileSync('notes.txt', " . Now appending some data in the existing one");
*/

/*
// Importind another file information
const util = require('./util.js');
//const readFile = require('./util.js')
//const add = require('./util.js');

const sum = util.add(6, (9 + 6));

console.log(util.name);
console.log(sum);
*/